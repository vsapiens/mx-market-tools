# mx-market-tools
# No environment variables required for core functionality.
# All data sources work offline with deterministic mock data.
# Install yahoo-finance2 for live market data (no API key needed).
